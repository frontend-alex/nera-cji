@{
    // Account views use different layouts:
    // - Login and AccessDenied: _Layout (public)
    // - Profile and other authenticated views: _LayoutAuth
    // Individual views can override this by setting Layout explicitly
    // Default to public layout if not explicitly set in the view
    Layout = "_Layout";
}
