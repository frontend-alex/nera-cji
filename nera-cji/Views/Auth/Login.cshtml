@{
    ViewData["Title"] = "Nera | Login";
}

<div class="flex flex-col gap-3 mt-10 xl::mt-0">
    <header class="lg:h-[60dvh] flex flex-col lg:flex-row justify-center items-center max-w-7xl mx-auto px-5 lg:px-0">
        <div class="flex flex-col gap-3 relative">
            <span class="cgi-logo"></span>
            <h1 class="text-4xl lg:text-6xl font-bold">Welcome to NERA-CGI</h1>
        </div>
    </header>
</div>